default:
  suites:
    default:
      contexts:
        - FeatureContext
        - AdminLoginContext
  extensions:
    Behat\MinkExtension:
      base_url: "http://127.0.0.1:8000"
      sessions:
        default:
          browserkit_http: ~
